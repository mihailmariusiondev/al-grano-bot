name: al-grano-bot
channels:
  - defaults
  - conda-forge
dependencies:
  - python=3.12
  - pip
  - pip:
      - python-telegram-bot
      - python-dotenv
      - openai
      - aiosqlite
      - youtube-transcript-api
      - python-readability
      - python-docx
      - PyPDF2
